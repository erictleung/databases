-- create a universities table
CREATE TABLE universities (
    name varchar(20) PRIMARY KEY,
    country varchar(20),
    state varchar(20),
    city varchar(20),
    motto varchar(50),
    established int
);
